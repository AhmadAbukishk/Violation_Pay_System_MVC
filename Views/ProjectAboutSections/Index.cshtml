@model IEnumerable<Traffic_Violation.Models.ProjectAboutSection>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Header)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subheader)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imgpath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Header)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subheader)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Imgpath)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Aboutid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Aboutid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Aboutid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
